# Dependencies
node_modules/

# Build
dist/
dist.backup/
build/
*.tsbuildinfo

# Config (local)
.env
.env.local
.sns-myagent/config.yaml
.sns-myagent/*.json
# Internal dev tracking directory (KANBAN / PHASE plans / patches)
.sns-myagent/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Internal dev tracking (not for GitHub)
KANBAN.md
ROADMAP-INTERNAL.md
TODO-INTERNAL.md
HANDOFF*.md
_HANDOFF_*.md
PROGRESS.md
PLAN-*.md
DOCS-AUDIT-TRACKER.md

# Test coverage
coverage/

# Logs
*.log
npm-debug.log*

# Binary artifacts
*.tgz
bin/
bin/snsagent*
!bin/.gitkeep

# Migration artifacts
package.json.orig

# Internal tracking
PLAN-*.md
PROGRESS.md
KANBAN.md
_HANDOFF_*
MASTER-PLAN*
*-DESIGN.md
*-INFRA.md

# Nested copy
sns-myagent/
PROGRESS.md
PLAN-*.md
bin/snsagent.js

# Plan and project-internal roadmap (Phase 1 commits only, then removed)
PLAN.md
plan.md

# Planning docs (internal, never push)
docs/plans/

# Local logs and runtime artifacts
logs/
*.log
*.pid

# Secrets (do NOT push)
.env
.env.*
!.env.example
secrets.env
*.secret

# Backup artifacts (local)
backups/
*.bak
*.orig
*.sql.gz
*.db.gz
*.dump

# OS / editor cruft (already present above but redundant safety)
*~
.*.swp
.directory

# Python (eval backend temp)
__pycache__/
*.pyc
.pytest_cache/

# Node (already have node_modules/, but extra safety)
.pnp/
.pnp.js

# Coverage
.nyc_output/

# Build cache (already have dist/, build/ but extra)
.cache/
.turbo/
.parcel-cache/

# Local config dirs (already have .sns-myagent/ but list explicit files)
.sns-myagent/config.json
.sns-myagent/state/
.sns-myagent/cache/
.sns-myagent/sessions/

# Per-user runtime state (out-of-tree but sometimes in repo for dev)
run/
tmp/

# Web project moved to separate repo (Reihantt6/sns-myagent-web)
web/
sns-myagent-web/

.npmrc
